apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: default
resources:
- nginx-app.yaml
secretGenerator:
  - name: nginx-app-values
    envs: 
    - nginx-app-secrets.yaml
configurations:
  - kustomizeconfig.yaml